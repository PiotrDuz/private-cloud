apiVersion: v1
kind: Service
metadata:
  name: {{ opencloud_service_name }}
  namespace: {{ opencloud_namespace }}
  labels:
    app.kubernetes.io/name: opencloud
spec:
  selector:
    app.kubernetes.io/name: opencloud
  ports:
    - name: http
      port: 9200
      targetPort: http
      protocol: TCP
