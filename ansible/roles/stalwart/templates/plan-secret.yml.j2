apiVersion: v1
kind: Secret
metadata:
  name: {{ stalwart_plan_secret_name }}
  namespace: {{ stalwart_namespace }}
  annotations:
    private-cloud.example/managed-by: ansible
type: Opaque
stringData:
  plan.ndjson: |
{{ lookup('template', 'plan.ndjson.j2') | indent(4, true) }}
