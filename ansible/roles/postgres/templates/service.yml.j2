apiVersion: v1
kind: Service
metadata:
  name: {{ postgres_service_name }}
  namespace: {{ postgres_namespace }}
spec:
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
