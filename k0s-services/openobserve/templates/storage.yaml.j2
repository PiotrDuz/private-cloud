apiVersion: v1
kind: PersistentVolume
metadata: {name: openobserve-data}
spec:
  capacity: {storage: 10Ti}
  accessModes: [ReadWriteOnce]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef: {name: openobserve-data, namespace: observability}
  local: {path: /tank/secure/backup/k0s/services/openobserve}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values: [{{ ansible_hostname | to_json }}]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: {name: openobserve-data, namespace: observability}
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ""
  volumeName: openobserve-data
  resources: {requests: {storage: 10Ti}}
