apiVersion: v1
kind: PersistentVolume
metadata:
  name: manticore-data
  labels:
    app.kubernetes.io/name: manticore
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: manticore-data
  local:
    path: {{ manticore_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ manticore_node_name | to_json }}
