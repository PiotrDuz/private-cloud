apiVersion: v1
kind: Service
metadata:
  name: manticore
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: manticore
spec:
  selector:
    app.kubernetes.io/name: manticore
  ports:
    - {name: http, port: 9308, targetPort: http, protocol: TCP}
