apiVersion: v1
kind: PersistentVolume
metadata:
  name: manticore-data
  labels:
    app.kubernetes.io/name: manticore
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: manticore-data
  local:
    path: /tank/secure/backup/k0s/services/manticore
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - replace-with-k0s-node
