apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: manticore-data
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: manticore
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Ti
  storageClassName: ""
  volumeName: manticore-data
