apiVersion: v1
kind: PersistentVolume
metadata:
  name: opencloud-data
  labels:
    app.kubernetes.io/name: opencloud
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: opencloud-data
  local:
    path: {{ opencloud_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ opencloud_node_name | to_json }}
