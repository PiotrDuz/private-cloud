apiVersion: v1
kind: PersistentVolume
metadata:
  name: opencloud-data
  labels:
    app.kubernetes.io/name: opencloud
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: opencloud-data
  local:
    path: /tank/secure/backup/k0s/services/opencloud
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - replace-with-k0s-node
