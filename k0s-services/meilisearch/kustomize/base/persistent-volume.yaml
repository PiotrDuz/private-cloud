apiVersion: v1
kind: PersistentVolume
metadata:
  name: meilisearch-data
  labels:
    app.kubernetes.io/name: meilisearch
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: meilisearch-data
  local:
    path: /tank/secure/backup/k0s/services/meilisearch
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - replace-with-k0s-node
