apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-data
  labels:
    app.kubernetes.io/name: immich
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: immich-data
  local:
    path: {{ immich_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ immich_node_name | to_json }}
