apiVersion: v1
kind: Service
metadata:
  name: immich-public
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: immich
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: immich
  ports:
    - {name: http, port: 2283, targetPort: http, nodePort: 30283, protocol: TCP}
