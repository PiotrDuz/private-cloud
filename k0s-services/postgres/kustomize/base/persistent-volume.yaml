apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-local-pv
  labels:
    app.kubernetes.io/name: postgres
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: postgres-data
  local:
    path: /tank/secure/backup/k0s/services/postgres
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - replace-with-k0s-node
