apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-local-pv
  labels:
    app.kubernetes.io/name: postgres
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: postgres-data
  local:
    path: {{ postgres_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ postgres_node_name | to_json }}
