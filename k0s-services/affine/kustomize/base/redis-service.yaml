apiVersion: v1
kind: Service
metadata:
  name: affine-redis
  namespace: private-cloud
spec:
  selector: {app.kubernetes.io/name: affine-redis}
  ports:
    - {name: redis, port: 6379, targetPort: redis, protocol: TCP}
