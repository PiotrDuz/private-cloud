apiVersion: v1
kind: PersistentVolume
metadata:
  name: affine-data
  labels:
    app.kubernetes.io/name: affine
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: affine-data
  local:
    path: /tank/secure/backup/k0s/services/affine
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - replace-with-k0s-node
