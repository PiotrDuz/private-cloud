apiVersion: v1
kind: PersistentVolume
metadata:
  name: tika-data
  labels:
    app.kubernetes.io/name: tika
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: tika-data
  local:
    path: {{ tika_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ tika_node_name | to_json }}
