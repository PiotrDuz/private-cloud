apiVersion: v1
kind: PersistentVolume
metadata:
  name: stalwart-data
  labels:
    app.kubernetes.io/name: stalwart
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: stalwart-data
  local:
    path: {{ stalwart_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ stalwart_node_name | to_json }}
