apiVersion: v1
kind: Service
metadata:
  name: stalwart-headless
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: stalwart
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: stalwart
  ports:
    - {name: management, port: 8080, targetPort: management, protocol: TCP}
