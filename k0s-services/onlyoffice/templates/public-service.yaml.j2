apiVersion: v1
kind: Service
metadata:
  name: onlyoffice-public
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: onlyoffice
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: onlyoffice
  ports:
    - {name: http, port: 80, targetPort: http, nodePort: 30082, protocol: TCP}
