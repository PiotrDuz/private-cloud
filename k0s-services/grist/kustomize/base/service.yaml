apiVersion: v1
kind: Service
metadata:
  name: grist
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: grist
spec:
  selector:
    app.kubernetes.io/name: grist
  ports:
    - {name: http, port: 8484, targetPort: http, protocol: TCP}
