apiVersion: v1
kind: PersistentVolume
metadata:
  name: grist-data
  labels:
    app.kubernetes.io/name: grist
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: grist-data
  local:
    path: {{ grist_dataset_mountpoint | to_json }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - {{ grist_node_name | to_json }}
