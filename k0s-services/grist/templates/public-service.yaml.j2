apiVersion: v1
kind: Service
metadata:
  name: grist-public
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: grist
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: grist
  ports:
    - {name: http, port: 8484, targetPort: http, nodePort: 30848, protocol: TCP}
