apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bleve-data
  namespace: private-cloud
  labels:
    app.kubernetes.io/name: bleve
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Ti
  storageClassName: ""
  volumeName: bleve-data
