apiVersion: v1
kind: PersistentVolume
metadata:
  name: bleve-data
  labels:
    app.kubernetes.io/name: bleve
spec:
  capacity:
    storage: 10Ti
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  claimRef:
    namespace: private-cloud
    name: bleve-data
  local:
    path: /tank/secure/backup/k0s/services/bleve
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - replace-with-k0s-node
